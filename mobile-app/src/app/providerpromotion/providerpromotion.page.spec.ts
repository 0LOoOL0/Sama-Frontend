import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ProviderpromotionPage } from './providerpromotion.page';

describe('ProviderpromotionPage', () => {
  let component: ProviderpromotionPage;
  let fixture: ComponentFixture<ProviderpromotionPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ProviderpromotionPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

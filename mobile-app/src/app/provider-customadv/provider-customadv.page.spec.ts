import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ProviderCustomadvPage } from './provider-customadv.page';

describe('ProviderCustomadvPage', () => {
  let component: ProviderCustomadvPage;
  let fixture: ComponentFixture<ProviderCustomadvPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ProviderCustomadvPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

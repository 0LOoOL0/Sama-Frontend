import { ComponentFixture, TestBed } from '@angular/core/testing';
import { NodigtialcardPage } from './nodigtialcard.page';

describe('NodigtialcardPage', () => {
  let component: NodigtialcardPage;
  let fixture: ComponentFixture<NodigtialcardPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(NodigtialcardPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

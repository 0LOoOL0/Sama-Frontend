import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ProviderprofilesetupPage } from './providerprofilesetup.page';

describe('ProviderprofilesetupPage', () => {
  let component: ProviderprofilesetupPage;
  let fixture: ComponentFixture<ProviderprofilesetupPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ProviderprofilesetupPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AdoptionpetinfoPage } from './adoptionpetinfo.page';

describe('AdoptionpetinfoPage', () => {
  let component: AdoptionpetinfoPage;
  let fixture: ComponentFixture<AdoptionpetinfoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AdoptionpetinfoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

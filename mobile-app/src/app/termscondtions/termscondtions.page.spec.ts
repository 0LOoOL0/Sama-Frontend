import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TermscondtionsPage } from './termscondtions.page';

describe('TermscondtionsPage', () => {
  let component: TermscondtionsPage;
  let fixture: ComponentFixture<TermscondtionsPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TermscondtionsPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
